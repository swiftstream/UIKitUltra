# TODO

Governance backlog for documentation quality and safety.

1. Add architecture-ID references (`FC*`, `ST*`, `RT*`, etc.) into future pull request templates.
2. Add small source-linked examples to each skill doc as the framework evolves.
3. Add explicit observer-teardown guidance where notification-based flows are expanded.
4. Periodically audit extension overload sets for ambiguity across platform conditionals.
5. Re-validate runtime model when `ForEach` modification handling changes.
6. Systematically design standalone PUBLIC UIKitUltra skills for as many meaningful framework aspects as practical, including view composition, State/binding, constraints/layout, gestures, navigation, macOS `UList`, declarative controls, and other mature surfaces. Keep downstream usage skills distinct from LOCAL source-maintainer procedures, and prepare each PUBLIC package for later Swift Stream Skills federation with package-local licensing and self-contained consumer guidance.
